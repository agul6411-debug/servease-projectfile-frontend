import 'package:flutter/material.dart';

// ── PRIMARY GREENS ─────────────────────────────────────────────────
const kTeal = Color(0xFF2E7D32); // deep green  (was teal)
const kTealAccent = Color(0xFF66BB6A); // medium green accent
const kTealMid = Color(0xFF43A047); // mid green
const kTealLight = Color(0xFFE8F5E9); // very light green bg

// ── ACCENT ORANGE ──────────────────────────────────────────────────
const kGold = Color(0xFFFF6F00); // deep orange/amber (was gold)

// ── NEUTRALS ───────────────────────────────────────────────────────
const kDark = Color(0xFF1A1A1A); // near-black (unchanged)
const kBg = Color(0xFFF5F5F5); // light grey background (unchanged)
const kMuted = Color(0xFF757575);   // grey text (unchanged)